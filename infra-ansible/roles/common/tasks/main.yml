---
# Common system setup for all servers

- name: Update apt cache
  apt:
    update_cache: yes

- name: Install base packages
  apt:
    name:
      - curl
      - git
      - unzip
    state: present